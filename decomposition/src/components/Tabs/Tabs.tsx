
/**
 * @param {Array} NameGroup Массив групп новостей
 * @param {function} callback
 * Принимает массив из названий групп новостей NameGroupNews и функцию, которая будет вызвана при нажатии на ссылку.
 * Функция вернет id  группы новостей, которые содержатся в массиве данных NameGroupNews и вызовет изменение состояния родительского компонента. 
 */

export default function Tabs() {
    return(
        <></>
    )
}